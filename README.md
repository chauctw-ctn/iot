node -e "const m = require('mqtt'); const c = m.connect('ws://localhost:3000'); c.on('connect', () => { const p = { station_id: 'gw_render_test', display_name: 'Tram Test Qua Render Host', timestamp: '2026-07-01 16:40:00', metrics: { level: 4.88, flow: 105.2 } }; c.publish('telemetry/push', JSON.stringify(p), { qos: 0 }, () => { console.log('🟢 [RENDER_HOST_TEST] Da ban goi tin den Gateway thanh cong!'); c.end(); }); });"